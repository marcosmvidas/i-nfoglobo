// @flow 
import * as React from 'react';
import { Posts } from '../../components/apiPosts';
type Props = {
    
};
export const Post_s = (props: Props) => {
    return (
        <div>
            <Posts />
        </div>
    );
};
