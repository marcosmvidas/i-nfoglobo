// @flow 
import * as React from 'react';
import { Usuarios } from '../../components/apiUsers';
type Props = {
    
};
export const Usuario_s = (props: Props) => {
    return (
        <div>
            <Usuarios />
        </div>
    );
};
